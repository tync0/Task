class ProductEntity {
  final int? id;
  final String? title, description, category, image;

  ProductEntity({
    this.id,
    this.title,
    this.description,
    this.category,
    this.image,
  });
}
